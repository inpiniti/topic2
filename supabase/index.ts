import board from './board';
import daily from './daily';
import monthly from './monthly';
import yearly from './yearly';

const supabase = {
  board,
  daily,
  monthly,
  yearly,
};

export default supabase;

// ani_list 가 조회 되는지 테스트 해보고 싶다.
